name: Daily Stock Scan

on:
  schedule:
    - cron: "30 3 * * *"   # Runs daily at 9 AM IST
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"
      # … rest of your steps …
